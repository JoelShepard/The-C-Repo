#include <stdio.h>

int main(){
    int a,b;
    int mcd =1;
    int min;

    printf("inserisci un valore: ");
    scanf("%d", &a);
    printf("inserisci un altro valore: ");
    scanf("%d", &b);

    if (a < b){
        min = a;
    } else {
        min = b;
    }

    
}